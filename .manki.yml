auto_review: true
auto_approve: true

exclude_paths:
  - "*.lock"

instructions: |
  This is a TypeScript GitHub Action project.
  Focus on: correct GitHub API usage, proper error handling, TypeScript type safety.
  The action processes untrusted PR content — flag any potential prompt injection vectors.

memory:
  enabled: true
  repo: "manki-review/review-memory"

# Smoke test: route judge to OpenAI (Codex CLI OAuth path) for v5.0.0 verification.
models:
  judge: o4-mini
